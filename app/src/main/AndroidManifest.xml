<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.helloeib">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.NoActionBar">
        <activity android:name=".ImageWebViwerSearcherActivity"></activity>
        <activity
            android:name=".FormActivity"
            android:label="@string/title_activity_form_acitvity"
            android:theme="@style/Theme.AppCompat.NoActionBar"></activity>
        <activity android:name=".NavigationFoodActivity" />
        <activity android:name=".ActivityTestNavComponent" />
        <activity android:name=".ActivityWebViewPopupImages" />
        <activity android:name=".RecyclerFoodImageDialogFragment" />
        <activity
            android:name=".DataFoodies"
            tools:ignore="Instantiatable" />
        <activity android:name=".FoodActivity" />
        <activity android:name=".LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".MainActivity"
            android:noHistory="false"
            android:screenOrientation="portrait" />
    </application>

</manifest>